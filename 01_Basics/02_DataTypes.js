"use strict" // treat all js code as new version;

/* 
    1. Number -> int, bigInt etc;
    2. String -> "abcd..";
    3. Boolean -> true/false;
    4. null -> It is a stand alone value, i.e. it means the variable is initialized and 
                has been assigned a null value i.e. empty value;
    5. undefined -> means that variable is initialized but the value is not assigned.
    6. Symbol -> used to find uniqueness, used in react, etc;

    7. Object ->
*/

//x = 10; // in strict mode we cannot define a variable like this.
//console.log(x);

console.log(typeof("Anmol"));
console.log(typeof(null));
console.log(typeof(undefined));